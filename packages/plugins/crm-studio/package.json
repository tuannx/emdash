{
	"name": "@aikit/crm-studio",
	"version": "0.1.0",
	"description": "Safety-first CRM Growth Studio for EmDash users, profiles, segments, programs, scoring, and measurement.",
	"type": "module",
	"main": "dist/index.js",
	"exports": {
		".": {
			"types": "./dist/index.d.ts",
			"default": "./dist/index.js"
		},
		"./sandbox": "./dist/sandbox.mjs"
	},
	"files": [
		"dist",
		"docs",
		"README.md"
	],
	"scripts": {
		"build": "tsc && tsdown src/sandbox-entry.ts --format esm --platform browser --target es2022 --out-dir dist --no-clean --sourcemap && cp dist/sandbox-entry.js dist/sandbox.mjs",
		"test": "pnpm run build && node test/domain.test.mjs && node test/file-config.test.mjs && node test/scoring.test.mjs && node test/statistics.test.mjs && node test/tracking.test.mjs && node test/sandbox-routes.test.mjs && node test/growth-routes.test.mjs && node test/sandbox-bundle.test.mjs",
		"typecheck": "tsgo --noEmit"
	},
	"keywords": [
		"emdash",
		"crm",
		"growth",
		"segments",
		"audience"
	],
	"license": "MIT",
	"peerDependencies": {
		"emdash": "workspace:>=0.29.0"
	},
	"dependencies": {
		"@emdash-cms/blocks": "workspace:*"
	},
	"devDependencies": {
		"tsdown": "catalog:",
		"typescript": "catalog:"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/tuannx/emdash.git",
		"directory": "packages/plugins/crm-studio"
	}
}
